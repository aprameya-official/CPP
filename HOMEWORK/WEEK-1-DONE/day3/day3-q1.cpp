#include <iostream>
using namespace std;

int main() {
    int a, b;

    // Input: Read two integer values
    cin >> a >> b;

    // Calculate the product of the two values
    int product = a * b;

    // Output: Display the product
    cout << product;

    return 0;
}
